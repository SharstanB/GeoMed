function showModal(dialog) {
    //if (!dialog.open) {
    //    dialog.showModal();
    //}
    $(dialog).modal('show');
}

function closeModal(dialog) {
    //if (!dialog.open) {
    //    dialog.showModal();
    //}
    $(dialog).modal('hide');
}